就相当于token
jwt由三部分组成
1.时间
2.签名
3.用户

后端需要一个秘钥来对jwt进行加密
前端往后端发登录请求,登录成功返回jwt(有效日期+签名+用户)所组成的jwt然后加密再返回给前端,下次请求的时候直接带着jwt来
如果前端的jwt失效了,往后端发请求,后端对jwt进行解密,如果有效日期到期了就提醒重新登录

jwt最好放在http的请求头Authorization字段里面